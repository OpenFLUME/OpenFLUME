/** Physical constants shared across core modules (single source of truth —
 *  the value was previously hardcoded independently in three files). */

/** Universal gas constant [J/(mol·K)], CODATA 2018 exact value. */
export const R_UNIVERSAL = 8.314462618;
