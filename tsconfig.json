{
  // Solution-style root: editors resolve each file through the project that
  // owns it. `npm run typecheck` checks all three.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.test.json" }
  ]
}
